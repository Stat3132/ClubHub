<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>All Events</title>
</head>
<body>
  <h1>Upcoming Events</h1>

  <% if (events.length > 0) { %>
    <ul>
      <% events.forEach(event => { %>
        <li>
          <strong><%= event.name %></strong> – <%= event.date %> <br>
          Hosted by: <%= event.hostedBy %><br>
          <%= event.description %>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No events found.</p>
  <% } %>

  <p><a href="/home">← Back to Home</a></p>
</body>
</html>
